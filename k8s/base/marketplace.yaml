# Marketplace Service Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: marketplace
  namespace: petpay
  labels:
    app: marketplace
    app.kubernetes.io/name: marketplace
    app.kubernetes.io/part-of: petpay-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: marketplace
  template:
    metadata:
      labels:
        app: marketplace
        app.kubernetes.io/name: marketplace
        app.kubernetes.io/part-of: petpay-app
    spec:
      containers:
        - name: marketplace
          image: petpay-app-marketplace:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: marketplace-config
            - secretRef:
                name: petpay-secrets
          env:
            - name: DB_HOST
              value: "dev-postgres.petpay-dev.svc.cluster.local"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: petpay-secrets
                  key: DB_USER
                  optional: true
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: petpay-secrets
                  key: DB_PASSWORD
                  optional: true
            - name: DB_NAME
              value: "petpay"
            - name: DB_PORT
              value: "5432"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: marketplace
                topologyKey: kubernetes.io/hostname
---
# Marketplace Service - ClusterIP (internal communication)
apiVersion: v1
kind: Service
metadata:
  name: marketplace
  namespace: petpay
  labels:
    app: marketplace
    app.kubernetes.io/part-of: petpay-app
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app: marketplace
