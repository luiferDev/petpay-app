{
  "name": "identity",
  "module": "index.ts",
  "type": "module",
  "private": true,
"scripts": {
    "start": "bun run index.ts",
    "dev": "bun --watch index.ts"  
    // Usa --watch para reiniciar automáticamente al guardar cambios
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.3",
    "@types/node": "^24.7.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
