File: config.yml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
  - role: worker
  - role: worker
  - role: worker
  - role: worker

    # Esta configuración es para MacOs y Windows
    # para linux es solo hacer docker inspect al control-plane
    # y obtenemos la IP y hacemos la petición seguida del puerto elegido
    # extraPortMapping:
    # - containerPort: 30001
    # hostPort: 8089
    # protocol: TCP